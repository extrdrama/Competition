# 凭迹 CredWatch · 对比优势与基准

评审会本能地问一句："别人也做密钥扫描（gitleaks / trufflehog），你凭什么不一样？"
本文档正面回答这个问题——诚实说明差异，不贬低对方，也不夸大自己。

---

## 一、定位差异

| 维度 | gitleaks / trufflehog 等通用工具 | 本系统 CredWatch |
| --- | --- | --- |
| **定位** | 开发者本地 / CI 的单仓库密钥扫描 | 面向**公开渠道**的持续监测平台 |
| **输入** | 一个 Git 仓库 / 一批文件 | 15 类公开渠道（代码托管、镜像、包仓库、App 等） |
| **输出** | 命中列表（文件 + 行号 + 规则名） | 去重后的凭据 + 暴露面 + 归属主体 + 处置建议 + MTTD |
| **目标指标** | 覆盖率 / 漏报率 | 发现数量 + 发现延迟（MTTD）+ 误报控制 |
| **是否关联** | 不做凭据对关联 | 识别"可组合利用"的凭据对 |
| **是否学习** | 规则写死 | 可从人工反馈中校准评分权重 |
| **是否验活** | trufflehog 部分支持 | 16 个只读验证器（云签名自实现） |

**一句话**：通用工具回答"这个仓库里有没有密钥"，本系统回答
"**全网公开渠道上，谁的、什么样的、是否仍可用的凭据正在泄露，多久了，怎么处置**"。
两者是不同层次的问题，本系统的目标更接近赛题的"云上凭据泄露的自动化检测"。

---

## 二、能力对比矩阵

| 能力 | gitleaks | trufflehog | 本系统 |
| --- | --- | --- | --- |
| 正则规则检测 | ✅ | ✅ | ✅（195 条自研规则） |
| 熵检测 | ✅ | ✅ | ✅ |
| 占位符/示例值过滤 | 部分 | 部分 | ✅（两级过滤） |
| 可解释评分（为什么判为真） | ❌ | ❌ | ✅（逐特征贡献） |
| 评分权重自适应学习 | ❌ | ❌ | ✅（逻辑回归 + 先验锚定） |
| 凭据对关联（组合风险） | ❌ | ❌ | ✅ |
| 指纹去重 + 跨渠道关联 | ❌ | 部分 | ✅ |
| 只读活性验证 | ❌ | ✅（部分） | ✅（16 个，云签名自实现） |
| 归属识别 + 披露建议 | ❌ | ❌ | ✅ |
| 处置建议（含可复制代码片段） | ❌ | ❌ | ✅ |
| 容器镜像构建历史检测 | ❌ | 部分 | ✅ |
| 渠道脚手架（一条命令加渠道） | ❌ | 部分 | ✅ |
| MTTD 时效度量 | ❌ | ❌ | ✅ |
| 规则库自检（lint） | ❌ | 部分 | ✅ |
| 规则自动归纳 | ❌ | ❌ | ✅ |

> 说明：上表依据各项目公开文档的**通常理解**填写，具体以各自最新版本为准。
> 本系统不依赖上述任何项目的代码与规则文件（见《引用与开源组件说明》）。

---

## 三、性能基准（可复现）

运行 `python -m credwatch bench --documents 2000`，语料由固定随机种子
**在内存中即时生成**（约 25% 为无凭据噪声文档）。此基准仅度量吞吐与
内存等性能指标，与凭据真实性主张无关——真实渠道的检测数据见
《测试报告》第八节：

| 指标 | 数值 |
| --- | --- |
| 语料规模 | 2000 份文档 / 864.6 KB |
| 注入凭据 | 9,607 条 |
| **总耗时** | **14.4 秒** |
| **吞吐量** | **138.9 文档/秒** |
| **峰值内存** | **54.1 MB** |

### 各阶段耗时

| 阶段 | 耗时 | 占比 |
| --- | --- | --- |
| 结构化解析 | 348 ms | 2.4% |
| 规则匹配 | 3,096 ms | 21.5% |
| 熵检测 | 422 ms | 2.9% |
| 概率评分 | 1,904 ms | 13.2% |
| 其他（去重/存储/关联） | 约 8.6 s | 60.0% |

> "其他"占大头是因为基准模式逐文档做指纹去重与 SQLite 写入；
> 生产常驻模式下，游标增量会让单轮处理的文档量远小于全量，
> 且多渠道路径可用 `--workers` 并发。

### 命中质量（按风险等级）

| 等级 | 命中数 | 说明 |
| --- | --- | --- |
| critical | 4,437 | 注入的云密钥/平台令牌，全部命中 |
| high | 5,159 | 注入的口令/密钥，全部命中 |
| medium | 4 | 兜底规则 |
| low | 1,480 | 宽泛的"内网 IP + 口令关键词"上下文提示 |

**关键结论**：在 critical + high 两个等级上，召回 **99.9%**（9,596 / 9,607，
差额来自同口令在多处出现被正确去重），且无等级误判。
low 等级是**故意保留**的宽泛线索规则（内网 IP + 口令关键词共现），
其定位是"线索"而非"告警"，默认在报告里与告警分级展示。

---

## 四、为什么"发现凭据数量"能站得住

赛题评分项里有"发现凭据的数量"。数量本身不是目的，**真实的数量**才是。

我们的做法：

1. **指纹去重**：同一密钥被 8 个文件转载，算作"1 个凭据、8 个暴露位置"，
   而不是虚报成 8 个凭据——评审抽查不会翻车；
2. **占位符过滤**：`your_key_here`、`<TOKEN>`、示例 DSN 一律不计入；
3. **只读验活**：能确认"仍然有效"的凭据单独标注，与"疑似"区分；
4. **降级线索与告警分级**：低级别的上下文提示不算入"发现凭据"的口径。

这样报出的数量，是**经得起逐条核查**的数量。

---

## 五、诚实的局限

不回避短板，评审问起来也答得清：

1. **`doc_share` / `search_engine` 依赖第三方搜索接口稳定性**，默认关闭、可配置；
2. **华为云 / Google API Key 无法自动验活**，明确返回"待人工复核"；
3. **加密的 wxapkg 包不支持**，解析器会明确报错而非静默失败；
4. **规则库仍需扩充**——195 条覆盖主流平台，长尾平台靠规则归纳逐步补；
5. **基准中的"其他"耗时偏高**，说明 I/O 与去重环节还有优化空间（后续方向）。
